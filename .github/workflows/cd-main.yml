name: CD — Production

on:
  push:
    branches:
      - main

env:
  NODE_VERSION: '22'
  PNPM_VERSION: '8'
  APP_NAME: 'web'

jobs:
  # -------------------------------------------------------
  # JOB 1 — Full checks (strictest for production)
  # -------------------------------------------------------
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm turbo run lint --filter=${{ env.APP_NAME }}

      - name: Typecheck
        run: pnpm turbo run typecheck --filter=${{ env.APP_NAME }}

      - name: Test
        run: pnpm turbo run test --filter=${{ env.APP_NAME }}

      - name: Build check
        run: pnpm turbo run build --filter=${{ env.APP_NAME }}
        env:
          VITE_APP_ENV: production
          VITE_API_URL: ${{ secrets.PROD_API_URL }}

  # -------------------------------------------------------
  # JOB 2 — Deploy to production (needs approval)
  # -------------------------------------------------------
  deploy-production:
    runs-on: ubuntu-latest
    needs: checks
    environment:
      name: production # ← set required reviewers in GitHub
      url: ${{ vars.PROD_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm turbo run build --filter=${{ env.APP_NAME }}
        env:
          VITE_APP_ENV: production
          VITE_API_URL: ${{ secrets.PROD_API_URL }}

      # -------------------------------------------------------
      # DEPLOY STEP — replace with your platform
      # -------------------------------------------------------

      # OPTION A: Vercel
      # - name: Deploy to Vercel (Production)
      #   uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     working-directory: apps/web
      #     vercel-args: '--prod'

      # OPTION B: Netlify
      # - name: Deploy to Netlify (Production)
      #   uses: nwtgck/actions-netlify@v3
      #   with:
      #     publish-dir: apps/web/dist
      #     production-deploy: true
      #     deploy-message: "Production release"
      #   env:
      #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      # OPTION C: AWS S3 + CloudFront
      # - name: Configure AWS
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ap-southeast-1
      # - name: Deploy to S3
      #   run: aws s3 sync apps/web/dist s3://${{ secrets.PROD_S3_BUCKET }} --delete
      # - name: Invalidate CloudFront cache
      #   run: aws cloudfront create-invalidation --distribution-id ${{ secrets.PROD_CF_ID }} --paths "/*"

      # OPTION D: VPS via SSH
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.PROD_SSH_HOST }}
      #     username: ${{ secrets.PROD_SSH_USER }}
      #     key: ${{ secrets.PROD_SSH_KEY }}
      #     script: |
      #       cd /var/www/prod
      #       git pull origin main
      #       pnpm install
      #       pnpm turbo run build --filter=web
      #       pm2 restart prod-app

      - name: Notify on Success
        run: echo "✅ Production deployment successful!"
